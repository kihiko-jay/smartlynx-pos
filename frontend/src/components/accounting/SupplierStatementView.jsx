import { useEffect, useState } from "react";
import { accountingAPI, productsAPI, fmtKES } from "../../api/client";
export default function SupplierStatementView(){const [supplierId,setSupplierId]=useState("");const [suppliers,setSuppliers]=useState([]);const [data,setData]=useState(null);useEffect(()=>{productsAPI.suppliers().then(r=>setSuppliers(r.items||r||[])).catch(()=>{});},[]);useEffect(()=>{if(supplierId) accountingAPI.supplierStatement(supplierId).then(setData).catch(()=>setData(null));},[supplierId]);return <div><select value={supplierId} onChange={e=>setSupplierId(e.target.value)}><option value="">Select supplier</option>{suppliers.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select>{data?<table style={{width:"100%"}}><thead><tr><th>Date</th><th>Type</th><th>Ref</th><th>Debit</th><th>Credit</th><th>Balance</th></tr></thead><tbody>{data.rows.map((r,i)=><tr key={i}><td>{r.date}</td><td>{r.type}</td><td>{r.reference}</td><td>{fmtKES(r.debit)}</td><td>{fmtKES(r.credit)}</td><td>{fmtKES(r.running_balance)}</td></tr>)}</tbody></table>:<div>Select supplier to view statement</div>}</div>}